# A shell script committed with CRLF fails on Linux with a stray carriage
# return after the shebang, and this repository is developed on Windows.
# `core.autocrlf` stores LF here, but it is a local setting; this is not.
*.sh text eol=lf
